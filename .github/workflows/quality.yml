# Workflow name
name: 'Code Quality'

# Event for the workflow
on: pull_request

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# List of jobs
jobs:
  lint:
    name: Lint
    # Operating System
    runs-on: ubuntu-latest
    # Job steps
    steps:
      - uses: hsnali/starter/.github/workflows/install.yml@v1
        # 👇 Lint
      - name: Lint
        run: yarn lint
  test:
    name: Test
    # Operating System
    runs-on: ubuntu-latest
    # Job steps
    steps:
      - uses: hsnali/starter/.github/workflows/install.yml@v1
        # 👇 Lint and test code
      - name: Test Storybook
        run: yarn storybook:test:coverage
      - name: Test Vite
        run: yarn test:coverage
      - name: Coverage
        uses: codecov/codecov-action@v3
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

